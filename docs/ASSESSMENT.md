# Assessment

Checking modes are `LOCAL_PRACTICE`, `SERVER_VERIFIED`, `MANUAL_REVIEW`, and `MIXED`. Configurations version evaluator identity and define score, attempts, feedback, and reveal policy. Secret authoritative answers never ship to clients. Offline drafts are local writes; submitted attempts are immutable and server receipt time is authoritative. Local grades are non-authoritative.