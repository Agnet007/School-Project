# ADR 0005: Built-in plugin system

Status: Accepted.

Plugins implement narrow capability contracts and register statically. The host exposes restricted rendering, events, snapshots, assets, and deterministic randomness. Runtime-uploaded JavaScript and a marketplace are deferred for security.